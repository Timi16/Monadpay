[profile.default]
src = "contracts"
out = "contracts/out"
libs = ["lib"]
solc = "0.8.24"

[rpc_endpoints]
monad_testnet = "${MONAD_RPC_URL}"

[etherscan]
monad_testnet = { key = "${ETHERSCAN_API_KEY}", url = "${EXPLORER_URL}" }
